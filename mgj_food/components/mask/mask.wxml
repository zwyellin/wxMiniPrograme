
<view   catchtouchmove="move" >
  <view class="mask" catch:tap="maskclick"></view>
  <!--内容延迟出现，避免混插-->
  <view class="{{position}}" wx:if="{{contentShow}}">
      
       <slot></slot>
  </view>
</view>
